apiVersion: kbld.k14s.io/v1alpha1
kind: Config
#! build image
sources:
- image: hello-app-image-build-me
  path: temp/src/hello-app
  pack:
    build:
      builder: paketobuildpacks/builder
#! publish (push) image
destinations:
- image: hello-app-image-build-me
  newImage: localhost:5001/gitopscon/hello-app
